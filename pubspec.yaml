name: wait_med
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: "^3.8.0"

dependencies:
  flutter:
    sdk: flutter

  flutter_map: ^7.0.0   # check for latest version
  latlong2: ^0.9.0
  firebase_core: ^2.24.0
  cloud_firestore: ^4.17.5



dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  get: ^4.6.5

flutter:
  uses-material-design: true
  assets:
    - assets/images/